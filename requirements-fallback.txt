# =============================================================================
# Fallback stack for environments where requirements.txt cannot be installed
# (Python 3.13+, numpy 2.x pre-installed, ...). Range pins only, so pip can
# always find wheels. The notebooks use this automatically if the primary
# install fails. Verified against the same smoke test as the primary stack.
# =============================================================================

ultralytics>=8.3.200,<9
albumentations>=1.4.4,<2.0        # 2.x renamed the CoarseDropout arguments
opencv-python-headless>=4.9.0.80,<5
numpy>=1.26.4,<3
Pillow>=10.3.0,<12
pandas>=2.2.2
PyYAML>=6.0.1
matplotlib>=3.8.4
seaborn>=0.13.2
scikit-learn>=1.5.0
tqdm>=4.66.4

gradio>=5.49,<6                   # gradio 5 manages its own fastapi/starlette pins

onnx>=1.16.0
onnxruntime>=1.18.0

kaggle>=2.2.4,<3
