# Deployment backend only (Render free tier: no GPU, 512 MB RAM).
# Deliberately NO torch / ultralytics / opencv - inference is ONNX Runtime on CPU
# and preprocessing is Pillow + numpy. Range pins so pip always finds wheels,
# whatever Python version the host runs (.python-version asks for 3.11).
fastapi>=0.115,<1
uvicorn[standard]>=0.32,<1
python-multipart>=0.0.20
onnxruntime>=1.20
numpy>=1.26
Pillow>=10.4
PyYAML>=6.0
